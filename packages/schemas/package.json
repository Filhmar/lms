{
  "name": "@rl/schemas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Zod schemas + inferred types for Resilient-Learn (frontend + backend). Source-only package: exports raw TS (erasable syntax only — consumed by bundlers and by Node 22 type stripping).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
